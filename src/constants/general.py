class Keys:
    """General-purpose keys used in the tool.
    Sorted alphabetically for easy reference.
    """
    COLUMNS = "columns"  # Key for specifying required columns
    COLS_REQUIRED = "columns_required"  # Key for specifying required columns
    COLS_OPTIONAL = "columns_optional"  # Key for specifying optional columns
    DTYPE = "dtype"  # Key for specifying data type
    KEYS = "keys"  # Key for specifying required keys
    KEYS_REQUIRED = "keys_required"  # Key for specifying required keys
    KEYS_OPTIONAL = "keys_optional"  # Key for specifying optional keys
    OPTIONAL = "optional"  # Key for specifying optional keys
    REQUIRED = "required"  # Key for specifying required keys
    SUMMARY = "summary"  # Key for summary metrics
    TIMESERIES = "timeseries"  # Key for timeseries data
    TIMESERIES_REQUIRED = "timeseries_required"  # Key for specifying required timeseries
    TIMESERIES_OPTIONAL = "timeseries_optional"  # Key for specifying optional timeseries


SEP = ":"  # Separator for any type of keys
